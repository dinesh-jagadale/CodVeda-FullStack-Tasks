
*(If you’re using MySQL instead, you can later replace MongoDB with MySQL steps.)*

---

# 📄 `terminal-commands/README.md`

```md
# Terminal Commands

Basic terminal commands are essential for navigating projects
and running development tools.

## Common Commands
```bash
cd folder-name     # Change directory
dir / ls           # List files
mkdir folder-name  # Create folder
code .             # Open VS Code
npm install        # Install dependencies
npm start          # Run project
git status         # Check git status
git add .          # Stage changes
git commit -m ""   # Commit changes
